# =============================================================================
# AutoSteer — Production Docker Compose
# =============================================================================
# Usage:
#   docker compose up -d                     # start all services
#   docker compose logs -f                   # tail logs
#   docker compose down -v                   # stop + remove volumes (!!)
#   docker compose -f docker-compose.yml -f docker-compose.dev.yml up -d  # dev
# =============================================================================

x-logging: &default-logging
  driver: "json-file"
  options:
    max-size: "10m"
    max-file: "3"

x-restart: &restart-policy
  restart: unless-stopped

# ── Services ──────────────────────────────────────────────────────────────────
services:

  # ── PostgreSQL 16 with pgvector ──────────────────────────────────────────────
  postgres:
    image: pgvector/pgvector:pg16
    container_name: autosteer-postgres
    <<: *restart-policy
    logging: *default-logging
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U autosteer -d autosteer"]
      interval: 10s
      timeout: 5s
      retries: 5
      start_period: 30s
    environment:
      POSTGRES_USER: autosteer
      POSTGRES_PASSWORD: ${POSTGRES_PASSWORD:-autosteer_dev}
      POSTGRES_DB: autosteer
    ports:
      - "5433:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
      - ./init-db.sql:/docker-entrypoint-initdb.d/init-db.sql:ro
    networks:
      - autosteer-network

  # ── Redis 7 ──────────────────────────────────────────────────────────────────
  redis:
    image: redis:7-alpine
    container_name: autosteer-redis
    <<: *restart-policy
    logging: *default-logging
    healthcheck:
      test: ["CMD", "redis-cli", "ping"]
      interval: 10s
      timeout: 3s
      retries: 5
      start_period: 5s
    ports:
      - "6379:6379"
    volumes:
      - redis_data:/data
    networks:
      - autosteer-network

  # ── Backend (FastAPI) ────────────────────────────────────────────────────────
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: autosteer-backend
    <<: *restart-policy
    logging: *default-logging
    healthcheck:
      test: ["CMD", "curl", "--fail", "http://localhost:8000/api/health"]
      interval: 30s
      timeout: 10s
      retries: 3
      start_period: 40s
    depends_on:
      postgres:
        condition: service_healthy
      redis:
        condition: service_healthy
    ports:
      - "8000:8000"
    env_file:
      - path: ./backend/.env
        required: false
    environment:
      DATABASE_URL: postgresql+asyncpg://autosteer:${POSTGRES_PASSWORD:-autosteer_dev}@postgres:5432/autosteer
      REDIS_URL: redis://redis:6379/0
      # Overridable via .env or host env — these defaults are dev-safe:
      OPENAI_API_KEY: ${OPENAI_API_KEY:-}
      ANTHROPIC_API_KEY: ${ANTHROPIC_API_KEY:-}
      AUTOSTEER_API_KEY: ${AUTOSTEER_API_KEY:-}
      CLERK_SECRET_KEY: ${CLERK_SECRET_KEY:-}
      INTEGRATION_ENCRYPTION_KEY: ${INTEGRATION_ENCRYPTION_KEY:-dev-change-me-in-production-use-openssl-rand}
      DEFAULT_LLM_PROVIDER: ${DEFAULT_LLM_PROVIDER:-openai}
      DEFAULT_LLM_MODEL: ${DEFAULT_LLM_MODEL:-gpt-4o}
      DEBUG: ${DEBUG:-false}
    networks:
      - autosteer-network

# ── Named Volumes ──────────────────────────────────────────────────────────────
volumes:
  postgres_data:
    name: autosteer_postgres_data
  redis_data:
    name: autosteer_redis_data

# ── Networks ───────────────────────────────────────────────────────────────────
networks:
  autosteer-network:
    name: autosteer-network
    driver: bridge
